import { CamionsPerZoneDirective } from './camions-per-zone.directive';

describe('CamionsPerZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new CamionsPerZoneDirective();
    expect(directive).toBeTruthy();
  });
});
