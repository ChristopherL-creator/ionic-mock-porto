import { PositivePositionPipe } from './positive-position.pipe';

describe('PositivePositionPipe', () => {
  it('create an instance', () => {
    const pipe = new PositivePositionPipe();
    expect(pipe).toBeTruthy();
  });
});
