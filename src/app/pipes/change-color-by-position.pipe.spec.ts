import { ChangeColorByPositionPipe } from './change-color-by-position.pipe';

describe('ChangeColorByPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeColorByPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
